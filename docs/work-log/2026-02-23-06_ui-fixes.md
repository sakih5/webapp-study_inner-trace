# 作業ログ 2026-02-23-06 — UI 微調整

## やったこと

### 行動記録テーブルの DATE 列削除

日付は日付ラベル（`formatDateLabel`）で表の上に表示されており、テーブル内に DATE 列が重複していたため削除。

| ファイル | 変更内容 |
|---------|---------|
| `src/components/log/LogRow.tsx` | 日付 `<td>`（`<input type="date">`）を削除 |
| `src/app/log/page.tsx` | テーブルヘッダーの `<th>Date</th>` を削除 |

### Inner Trace ロゴフォントの変更

ChatGPT 風のクリーンなサンセリフに合わせて変更。

| ファイル | 変更前 | 変更後 |
|---------|-------|-------|
| `src/components/shared/NavBar.tsx` | `text-[20px] font-black tracking-tight` | `text-[17px] font-semibold` |

## 決定事項

- DATE 列は廃止。日付情報は日付グループラベルのみで提供する
- ロゴは `font-semibold`（600）で統一。`font-black`（900）は重すぎてアプリのトーンに合わなかった
