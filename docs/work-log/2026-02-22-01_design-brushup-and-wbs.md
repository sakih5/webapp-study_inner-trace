# 作業ログ 2026-02-22

## やったこと

### 1. design.md の内容把握
- `docs/design.md`（設計書 v1.0）を読んで内容を確認

### 2. design.md のブラッシュアップ
以下の改善点を特定し、全て反映して v1.1 に更新した。

**仕様の明確化**
- 仮行の保存トリガー: `action`/`content` が必須のため、select変更だけではPOSTしない動作を明文化
- 日付変更時の行移動: 楽観的即時移動 + フェードアニメーション + 失敗時ロールバックを追加
- `sort_order` 採番ルール: `MAX(sort_order)+1` で末尾採番と明記
- 初期シード: Database Trigger の完全なSQL実装を追加

**技術的修正**
- `@supabase/auth-helpers-nextjs` → `@supabase/ssr` に変更（非推奨のため）
- Dockerfile をマルチステージビルドに刷新
- CORS 設定の実装を追加
- `visualViewport` API でモバイル仮想キーボード対応

**不足情報の補完**
- `useSettings` フック設計を新規追加
- Next.js ミドルウェアの完全実装を追加
- 楽観的更新失敗時のロールバック + 右下インジケーター連携を明記
- `buildRetroText` の完全実装を追加
- エラーコード一覧（5種類）を定義
- 環境変数一覧を包括的に整理
- ローカル開発手順を追加

**構成整理**
- 旧 §11（認証画面設計）を §9（認証設計）に統合
- NavBar の `onTabChange` コールバック削除 → `usePathname()` 内部利用に変更
- 目次をネストリスト形式に整理
- §13 今後の拡張候補に sort_order 並び替えUIを追記

### 3. WBS 作成
- `docs/wbs.md` にモック実装フェーズのWBSをチェックボックス形式で作成

## 決定事項

- **実装方針**: モック先行（ローカルステート + モックデータでフロント完成 → 後からバックエンド接続）
- **確認方法**: ローカル（`npm run dev`）で確認しながら進める
- **進め方**: ステップバイステップで都度確認を取りながら実装する

## 次回やること

**1.1 Next.jsプロジェクト初期化**から着手する（WBSの順序通り）。

```text
1.1 → 1.2 → 1.3 → 2.x → 3.x（行動記録画面）→ すり合わせ → 以降
```
